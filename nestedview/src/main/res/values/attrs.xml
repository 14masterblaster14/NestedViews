<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="CustomProgressBar">
        <attr name="backgroundColor" format="color" />
        <attr name="progressColor" format="color" />
        <attr name="title" format="string" />
        <attr name="titleColor" format="color" />
        <attr name="titleTextSize" format="dimension" />
        <attr name="unit" format="string" />
        <attr name="unitColor" format="color" />
        <attr name="unitTextSize" format="dimension" />
        <attr name="backgroundStrokeWidth" format="dimension" />
        <attr name="progressStrokeWidth" format="dimension" />
        <attr name="spaceAfterTitle" format="dimension" />
    </declare-styleable>

    <declare-styleable name="ValueBar">
        <!-- The height or (thickness) of the bar -->
        <attr name="barHeight" format="dimension" />
        <!-- Radius of the circle indicator -->
        <attr name="circleRadius" format="dimension" />
        <!-- Amount of space to the right of the bar, before the max value label-->
        <attr name="spaceAfterBar" format="dimension" />
        <!-- Text size of the value displayed inside the circle indicator -->
        <attr name="circleTextSize" format="dimension" />
        <!-- Text size of the max value label -->
        <attr name="maxValueTextSize" format="dimension" />
        <!-- Text size of the label, displayed above the bar -->
        <attr name="labelTextSize" format="dimension" />
        <!-- Color of the label text -->
        <attr name="labelTextColor" format="color" />
        <!-- Color of the max value label text -->
        <attr name="maxValueTextColor" format="color" />
        <!-- Color of the text inside the circle indicator -->
        <attr name="circleTextColor" format="color" />
        <!-- Base color of the bar (the portion that is not filled) -->
        <attr name="baseColor" format="color" />
        <!-- Fill color of the bar -->
        <attr name="fillColor" format="color" />
        <!-- Text to use for the label -->
        <attr name="labelText" format="string" />
    </declare-styleable>

</resources>